node_modules/
dist/
*.tsbuildinfo
.DS_Store
coverage/
*.log
.env
.env.*
# ...but the template is meant to be committed, and `.env.*` above would swallow it.
!.env.example

# SQLite, for when the example's in-memory Better Auth store is swapped for libsql.
# The -wal and -shm companions appear under WAL mode and are just as unwanted.
*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3

# direnv's materialised nix profile
.direnv/

# nix build output symlinks
result
result-*

# vendored reference source is committed; its build output is not
docs/repos/**/dist/
docs/repos/**/node_modules/
