# Vendored git subtree of Effect's source — 53 MB of somebody else's code. Any
# finding in here is theirs to fix and cannot be a leak of ours.
docs/repos/**

# Lockfiles are integrity hashes and registry URLs. They are long, high-entropy
# and entirely public, which is exactly the shape that trips a secret scanner.
pnpm-lock.yaml
**/pnpm-lock.yaml

node_modules/**
**/node_modules/**
dist/**
**/dist/**
